# Environment Variables Template

# -- Application Configuration --
NODE_ENV=development
PORT=3001

# -- Supabase Configuration --
# Frontend (public keys)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Backend (server-side keys)
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE=
SUPABASE_JWT_SECRET=

# -- Database Connection (for Kysely ORM) --
# Example: postgresql://postgres:your-password@db.your-project-ref.supabase.co:5432/postgres
DATABASE_URL=

# -- Feature Flags --
# Set to true to enable automatic database migrations on startup
FEATURE_MIGRATIONS=false
# Set to true to enable database seeding on startup
FEATURE_SEED_DB=false
# Set to true to enable Instance-specific features (if any remain)
FEATURE_INSTANCE=false

# -- Push Notifications (VAPID) --
# Generate with `npm run generate-vapid`
VAPID_PUBLIC_KEY=YOUR_PUBLIC_KEY_HERE
VAPID_PRIVATE_KEY=YOUR_PRIVATE_KEY_HERE
VAPID_EMAIL=admin@outdoorteam.com

# -- Security --
# Secret key for custom JWTs if not using Supabase Auth exclusively
JWT_SECRET=your-secret-key-change-in-production

# -- Mobile App (Capacitor) Configuration --
# Optional: If your mobile app needs to connect to a different backend URL
VITE_API_BASE_URL=
# Use HashRouter for Capacitor builds to avoid routing issues. Set to 'true' for mobile builds.
VITE_USE_HASH_ROUTER=false
